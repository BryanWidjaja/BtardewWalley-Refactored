package model.plants;

public class Beetroot extends Plant {
    public Beetroot(int x, int y, int growthTime, double price, boolean harvestable) {
        super('b', "Beetroot", x, y, growthTime, price, harvestable);
    }
}